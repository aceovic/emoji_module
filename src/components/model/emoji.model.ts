export class Emoji {

    title: string; 
    symbol: string;
    keywords: string;

    constructor(title: string, symbol: string, keywords: string) {
        this.title = title ; 
        this.symbol =symbol ;
        this.keywords = keywords ;
    }
}